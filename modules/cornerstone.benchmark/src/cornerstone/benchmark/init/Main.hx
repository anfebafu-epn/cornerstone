package cornerstone.benchmark.init;
class Main {
    public static function main() {

        // registro de todos los procesadores

        Type.getClass(Benchmark);
        //Type.getClass(TestExtern);
    }
}
